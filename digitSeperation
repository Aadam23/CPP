#include <iostream>
using namespace std;

int main() {
    int num;
    int remainder;
    
    cout << "Enter a number: ";
    cin >> num;
    
    while (num != 0) {
        remainder = num % 10; // 3
        num = num / 10;
        cout << remainder << endl;
    }
    
    return 0;
    
}
